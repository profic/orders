package orders;

public class Seller extends OrderEntry {
    // todo: encapsulate and make final

    public Seller(final int id, final int size, final int price) {
        super(id, size, price);
    }
}
